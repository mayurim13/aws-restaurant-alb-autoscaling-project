# aws-restaurant-alb-autoscaling-project
A scalable restaurant web application architecture deployed on AWS using Application Load Balancer (ALB). Auto Scaling group (ASG), EC2 instances, and custom VPC networking. The project demonstrates high availability, path based routing, traffic distribution and autoscaling across multiple EC2 instances.
